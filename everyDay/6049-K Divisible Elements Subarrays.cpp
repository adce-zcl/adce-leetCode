// 含最多 K 个可整除元素的子数组
#include <vector>
#include <unordered_map>
using namespace std;
class Solution 
{
public:
    vector<vector<int>> childNums;
    vector<int> path;

    int countDistinct(vector<int>& nums, int k, int p) 
    {
        int start = 0;
        int end = 0;
        
    }
};